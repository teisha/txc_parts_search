"use strict";const db=require("../util/database");module.exports=class{static fetchAllRequests(){return db.execute("SELECT  *   FROM  user_requests ur    INNER JOIN user_request_categories urc ON       ur.category_type_id = urc.category_type_id  ORDER BY user_request_id DESC  LIMIT 30")}static addRequests(e,t,r,s){return db.execute(" INSERT INTO user_requests (category_type_id,  request_text, created_by, user_id )  SELECT category_type_id, ?, ?, ?  FROM user_request_categories  WHERE category_type = ? ",[t,r,s,e])}};